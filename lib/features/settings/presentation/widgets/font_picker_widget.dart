import 'package:flutter/material.dart';

class FontPickerWidget extends StatelessWidget {
  const FontPickerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
