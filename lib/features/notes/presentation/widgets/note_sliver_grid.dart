import 'package:flutter/material.dart';

class NoteSliverGrid extends StatelessWidget {
  const NoteSliverGrid({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

